
@{
    ViewBag.Title = "课程类别管理";
}

@section Styles{
    <style>
        .content_flex {
            padding: 0 12px;
            display: flex;
        }

            .content_flex div {
                margin-right: 10px;
            }

        .fill_width {
            width: 100%;
        }
        .file-open input {
            position: absolute;
            right: 0;
            top: 0;
            opacity: 0;
        }

        .file-open:hover {
            background: #AADFFD;
            border-color: #78C3F3;
            color: #004974;
            text-decoration: none;
        }
    </style>
}

<div class="main-content">
    <div class="main-content-inner">
        <div class="breadcrumbs ace-save-state" id="breadcrumbs">
            <ul class="breadcrumb">
                <li>
                    <i class="menu-icon fa fa-database"></i>
                    <a href="#">基础数据管理</a>
                </li>
                <li class="active">课程信息管理</li>
            </ul><!-- /.breadcrumb -->

        </div>

        <div class="page-content">
            <div class="page-header">
                <h1>课程信息管理</h1>
            </div><!-- /.page-header -->
            <div class="row content_flex" style="margin-bottom:10px;">
                <!--<label class="col-sm-2 control-label no-padding-right" for="form-field-1">统计类型</label>-->
                <!--状态下拉菜单-->
                @*<div>
                        @Html.DropDownList("orderStatus", (List<SelectListItem>)ViewData["statusList"])
                    </div>*@
                <div>
                    <input id="search" placeholder="可搜索课程名称" style="width: 187px;color:#858585;padding: 3px 4px;height: 30px;border:1px solid #D5D5D5;" />
                    <label>时间：</label>从
                    <input id="startDate" onclick="WdatePicker({ dateFmt: 'yyyy-MM-dd HH:00:00' });" style="width: 187px;color:#858585;padding: 3px 4px;height: 30px;border:1px solid #D5D5D5;border-radius: 3px" />&nbsp;到
                    <input id="endDate" onclick="WdatePicker({ dateFmt: 'yyyy-MM-dd HH:00:00' });" style="width: 187px;color:#858585;padding: 3px 4px;height: 30px;border:1px solid #D5D5D5;border-radius: 3px" />
                    <button class="btn btn-primary btn-sm" onclick="selectClick()" style="margin-bottom:3px;">查询</button>

                    <button class="btn btn-primary btn-sm" style="margin-bottom:3px;" onclick="Add()">添加</button>
                    <a href="javascript:;" class="btn btn-primary btn-sm file-open"><input name="file" style="margin-bottom:3px;" type="file" id="file" onchange="AddCourse(this)" />选择文件</a>       
                    <button class="btn btn-primary btn-sm" style="margin-bottom:3px;" onclick="DownloadExcel()">下载模板</button>
                </div>
                @*<div class="col-sm-3">
                        <label>处理人</label>
                        <input id="createBy" style="width: 187px;color:#858585;padding: 3px 4px;height: 30px;border:1px solid #D5D5D5;" />
                    </div>
                    <div class="col-sm-3">
                        <label>创建时间</label>
                        <input id="txtDate" onClick="WdatePicker({ dateFmt: 'yyyy-MM-dd' });" style="width: 187px;color:#858585;padding: 3px 4px;height: 30px;border:1px solid #D5D5D5;" />
                    </div>*@

            </div>
            <div class="row">
                <div class="col-xs-12">

                    <table id="menu-table"></table>

                    <div id="menu-pager"></div>

                    <!-- PAGE CONTENT ENDS -->
                </div><!-- /.col -->
            </div><!-- /.row -->
            <div class="row">
                <div id="chart_AQI" style="width: 60%;height:300px;"></div>
            </div>
        </div><!-- /.page-content -->
    </div>
</div><!-- /.main-content -->
<!-- 模态框 -->
<div class="modal fade" id="myModal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                <h4 class="modal-title" id="myModalLabel">编辑课程信息</h4>
            </div>
            <div class="modal-body">

                <form class="form-horizontal">
                    <div class="form-group">
                        <input type="text" class="form-control" id="CourseId" placeholder="CourseId" style="display:none">
                        <label for="CourseCode" class="col-sm-2 control-label">课程代码:</label>
                        <div class="col-sm-4">
                            <input type="text" class="form-control" id="CourseCode" placeholder="课程代码">
                        </div>
                        <label for="CourseName" class="col-sm-2 control-label">课程名称:</label>
                        <div class="col-sm-4">
                            <input type="text" class="form-control" id="CourseName" placeholder="课程名称">
                        </div>
                    </div>
                    @*<div class="form-group">
                            <label for="CourseTeacher" class="col-sm-2 control-label">任课教师:</label>
                            <div class="col-sm-4">
                                <input type="text" class="form-control" id="CourseTeacher" placeholder="任课教师">
                            </div>
                            <label for="TeachingClass" class="col-sm-2 control-label">授课班级:</label>
                            <div class="col-sm-4">
                                <input type="text" class="form-control" id="TeachingClass" placeholder="授课班级">
                            </div>
                        </div>*@
                    <div class="form-group">
                        @*<label for="AuditOffice" class="col-sm-2 control-label">审核教研室:</label>
                            <div class="col-sm-4">
                                <input type="text" class="form-control" id="AuditOffice" placeholder="审核教研室">
                            </div>*@
                        <label for="CourseTypeCode" class="col-sm-2 control-label">课程类别:</label>
                        <div class="col-sm-4">
                            <input type="text" class="form-control" id="CourseTypeCode" placeholder="课程类别">
                        </div>

                        <label for="SchoolSmester" class="col-sm-2 control-label">学年学期:</label>
                        <div class="col-sm-4">
                            <input type="text" class="form-control" id="SchoolSmester" placeholder="学年学期">
                        </div>
                    </div>
                    <div class="form-group">
                        <label for="Remark" class="col-sm-2 control-label">备注:</label>
                        <div class="col-sm-4">
                            <input type="text" class="form-control" id="Remark" placeholder="备注">
                        </div>
                        <label for="SchoolSmester" class="col-sm-2 control-label">审核:</label>
                        <div class="col-sm-4">
                            <input type="text" class="form-control" id="AuditOfficeCode" placeholder="审核">
                        </div>
                    </div>
                </form>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-default" data-dismiss="modal">取消</button>
                <button type="button" class="btn btn-primary" onclick="saveMenu()">确认</button>
            </div>
        </div>
    </div>
</div>
@section Scripts{
    <!-- page specific plugin scripts -->
    <script src="~/assets/js/bootstrap-datepicker.min.js"></script>
    <script src="~/assets/js/jquery.jqGrid.min.js"></script>
    <script src="~/assets/js/grid.locale-en.js"></script>
    <script src="~/assets/js/ace-elements.min.js"></script>
    <script type="text/javascript" src="~/incubator-echarts-4.6.0/dist/echarts.min.js"></script>
    <script src="~/Scripts/My97DatePicker/WdatePicker.js" type="text/javascript"></script>
    <script type="text/javascript">
        if ('ontouchstart' in document.documentElement) document.write("<script src='assets/js/jquery.mobile.custom.min.js'>" + "<" + "/script>");
    </script>
    <script type="text/javascript">
        var editFlag = "";
        jQuery(function ($) {
            $('.date-picker').datepicker({
                autoclose: true,
                todayHighlight: true
            }).next().on(ace.click_event, function () {
                $(this).prev().focus();

            });

            //状态栏下拉改变事件
            $('#orderStatus').change(function () {
                var orderStatus = $('#orderStatus').val();//状态值
                var pageIndex = $("#menu-table").getGridParam('page');//当前页
                var pageSize = $("#menu-table").getGridParam('rowNum');//页数
                var postData = { "orderStatus": orderStatus, "pageIndex": pageIndex == undefined ? 1 : pageIndex, "pageSize": pageSize == undefined ? 10 : pageSize };
                $("#menu-table").jqGrid('setGridParam', {
                    datatype: 'json',
                    postData: postData, //发送数据
                    page: 1
                }).trigger("reloadGrid"); //重新载入
            });
        });

        //刷新表格
        function loadSiteDataList(data) {
            //var orderCodeId = $("#orderCode option:selected").val();
            //var createBy = $("#createBy option:selected").val();
            var search = $("#search").val();
            var createBy = $("#createBy").val();
            var txtDate = $("#txtDate").val();
            //获取查询时间
            var start = $("#startDate").val();
            var end = $("#endDate").val();

            var pageIndex = $("#menu-table").getGridParam('page');//当前页
            var pageSize = $("#menu-table").getGridParam('rowNum');//页数
            var postData = {
                "search": search,
                "pageIndex": pageIndex == undefined ? 1 : pageIndex,
                "pageSize": pageSize == undefined ? 10 : pageSize,
                "start": start,
                "end": end
            };

            //var postData = { "orderCodeId": orderCodeId, "createBy": createBy, "txtDate": txtDate, "pageIndex": pageIndex == undefined ? 1 : pageIndex, "pageSize": pageSize == undefined ? 10 : pageSize };
            $("#menu-table").jqGrid('setGridParam', {
                datatype: 'json',
                postData: postData, //发送数据
                page: 1
            }).trigger("reloadGrid"); //重新载入


        };

        //获取url参数
        function getQueryVariable(variable) {
            var query = window.location.search.substring(1);
            var vars = query.split("&");
            for (var i = 0; i < vars.length; i++) {
                var pair = vars[i].split("=");
                if (pair[0] == variable) { return pair[1]; }
            }
            return "";
        }

        jQuery(function ($) {
            var grid_selector = "#menu-table";
            var pager_selector = "#menu-pager";
            var parent_column = $(grid_selector).closest('[class*="col-"]');
            //resize to fit page size
            $(window).on('resize.jqGrid', function () {
                $(grid_selector).jqGrid('setGridWidth', parent_column.width());
            })

            //resize on sidebar collapse/expand
            $(document).on('settings.ace.jqGrid', function (ev, event_name, collapsed) {
                if (event_name === 'sidebar_collapsed' || event_name === 'main_container_fixed') {
                    //setTimeout is for webkit only to give time for DOM changes and then redraw!!!
                    setTimeout(function () {
                        $(grid_selector).jqGrid('setGridWidth', parent_column.width());
                    }, 20);
                }
            })

            //获取任务状态参数
            //var status = getQueryVariable("status");
            //操作栏按钮
            var buttonFormatter = function (value, grid, rows, state) {
                //h = "<button onclick=\"redirectUrl('/Order/Edit?OrderCode=" + rows.OrderCode + "')\">详情</button>";
                //h = "<button><a href='/Order/Edit?OrderCode=" + rows.OrderCode + "' style='color:#000;text-decoration:none;'>详情</a></button>";
                //console.log(rows);
                g = "<button onclick=\"editMenu('" + grid.rowId + "','" + rows + "' )\" >修改</button>";
                h = "<button onclick=\"deleteMenu('" + grid.rowId + "','" + rows.CourseId + "')\">删除</button>";
                return g + h;
            }

            jQuery(grid_selector).jqGrid({
                type: 'Get',
                url: '/Course/GetCourses',
                data: [],
                postData: {
                    "orderStatus": "1"
                },
                datatype: "json",
                caption: "课程列表查询",
                height: "100%",
                width: "100%",
                colNames: ["序号", "ID", "课程代码", "课程名称", "课程类别", "学年学期", "备注", "审核教研室", "创建时间", "操作人", "修改时间", "操作"],
                colModel: [//hidden: true
                    { name: 'RowNo', index: 'RowNo', width: 40 },
                    { name: "CourseId", index: "CourseId", width: 90, sorttype: "int", hidden: true },
                    { name: 'CourseCode', index: 'CourseCode', width: 120, sortable: true, editable: false },
                    { name: 'CourseName', index: 'CourseName', width: 120, sortable: true, editable: false },
                    { name: 'CourseTypeCode', index: 'CourseTypeCode', width: 150, editable: true, sortable: true },
                    { name: 'SchoolSmester', index: 'SchoolSmester', width: 150, editable: true, sortable: true },
                    { name: 'Remark', index: 'Remark', width: 150, editable: true, sortable: true },
                    { name: 'AuditOfficeCode', index: 'AuditOfficeCode', width: 150, editable: true, sortable: true },
                    { name: 'CreateTime', index: 'CreateTime', width: 150, editable: true, sortable: true },
                    { name: 'ModifyBy', index: 'ModifyBy', width: 150, editable: true, sortable: true },
                    { name: 'ModifyTime', index: 'ModifyTime', width: 150, editable: true, sortable: true },
                    {
                        name: 'myac', index: '', width: 110, fixed: true, sortable: false, resize: false,
                        formatter: buttonFormatter
                        //    function (value, grid, rows, state) {
                        //    e = "<a id=\"order_edit" + grid.rowId + "\" href=\"/Order/Edit?OrderCode=" + rows.OrderCode + "\" style=\"color:#f60;margin:5px;\" class=\"ace-icon fa fa-pencil blue\"></a>";
                        //    d = "<a id=\"order_del" + grid.rowId + "\" href=\"#\" style=\"color:#f60;margin:5px;\" class=\"ace-icon fa fa-trash-o red\"   onclick=\"DeleteOrder('" + rows.OrderCode + "')\"></a>";
                        //    a = "<a id=\"order_add" + grid.rowId + "\" href=\"#\" style=\"color:#f60;margin:5px;display:none;\" class=\"ace-icon fa fa-check green\"  onclick=\"SaveUserById(" + grid.OrderCode + ")\"></a>";
                        //    c = "<a id=\"order_cancel" + grid.rowId + "\" href=\"#\" style=\"color:#f60;margin:5px;display:none;\" class=\"ace-icon fa fa-times red\"   onclick=\"CancelUserById(" + grid.OrderCode + ")\"></a>";
                        //    return e + d + a + c;
                        //}
                    }
                ],
                //rownumbers: true,//在最左边是否显示序列号,自增长的
                viewrecords: true,//是否显示总记录数
                sortname: "CreateTime", //默认的排序字段
                sortorder: "desc",
                rowNum: 10,
                rowList: [10, 20, 30],
                recordtext: "从{0}到{1} 条，共 {2}条记录",
                pgtext: "第{0} 页，共 {1} 页",
                emptyrecords: "没有数据",
                loadtext: "加载中...",
                jsonReader: {
                    root: "rows",
                    repeatitems: false,
                    page: "page",
                    total: "pageCount",
                    records: "total"
                },
                pager: pager_selector,
                altRows: true,
                //toppager: true,
                multiselect: false,
                //multikey: "ctrlKey",
                multiboxonly: true,
                //editurl: "./dummy.php",//nothing is saved
                loadComplete: function () {
                    var table = this;
                    setTimeout(function () {
                        updatePagerIcons(table);
                        enableTooltips(table);
                    }, 0);
                },
                onSelectRow: function (id) {

                }
            });





            $(window).triggerHandler('resize.jqGrid');//trigger window resize to make the grid get the correct size

            jQuery(grid_selector)
                .navGrid(pager_selector, { edit: false, add: false, del: false, search: false });


            //replace icons with FontAwesome icons like above
            function updatePagerIcons(table) {
                var replacement =
                {
                    'ui-icon-seek-first': 'ace-icon fa fa-angle-double-left bigger-140',
                    'ui-icon-seek-prev': 'ace-icon fa fa-angle-left bigger-140',
                    'ui-icon-seek-next': 'ace-icon fa fa-angle-right bigger-140',
                    'ui-icon-seek-end': 'ace-icon fa fa-angle-double-right bigger-140'
                };
                $('.ui-pg-table:not(.navtable) > tbody > tr > .ui-pg-button > .ui-icon').each(function () {
                    var icon = $(this);
                    var $class = $.trim(icon.attr('class').replace('ui-icon', ''));

                    if ($class in replacement) icon.attr('class', 'ui-icon ' + replacement[$class]);
                })
            }

            function enableTooltips(table) {
                $('.navtable .ui-pg-button').tooltip({ container: 'body' });
                $(table).find('.ui-pg-div').tooltip({ container: 'body' });
            }

            //var selr = jQuery(grid_selector).jqGrid('getGridParam','selrow');

            $(document).one('ajaxloadstart.page', function (e) {
                $.jgrid.gridDestroy(grid_selector);
                $('.ui-jqdialog').remove();
            });
        });

        function selectClick() {
            loadSiteDataList();
        }



    </script>

    <script type="text/javascript">




        function Add() {
            //清空输入框
            $('#CourseId').val("");
            $('#CourseCode').val("");
            $('#CourseName').val("");
            //$('#TeachingClass').val("");
            $('#AuditOfficeCode').val("");
            $('#CourseTypeCode').val("");
            $('#SchoolSmester').val("");
            $('#Remark').val("");
            editFlag = "Add";
            //打开模态框
            $('#myModal').modal('show');
        }

        //编辑菜单
        function editMenu(id) {
            debugger;
            //获取编辑行的数据
            var course_data = jQuery("#menu-table").jqGrid('getRowData', id);
            editFlag = "Edit";
            console.log(course_data);
            //获取编辑任务相关数据
            $.ajax({
                type: "GET",
                url: "/Course/GetCourseByCode?courseCode=" + course_data.CourseCode,
                dataType: "json",
                //data: { "ID": user_data.ID, "UserCode": user_data.UserCode, "UserName": user_data.UserName, "UserRoles": user_data.RoleName, "HireDate": user_data.HireDate, "Email": user_data.Email, "Addr": user_data.Addr, "opType": userID == "" ? "Add" : "modify" },
                success: function (data) {
                    debugger;
                    if (data.IsSuccess) {

                        var courseInfo = data.ResultValue;

                        //设置之前数据为前显示的项
                        $('#CourseId').val(courseInfo.CourseId);
                        $('#CourseCode').val(courseInfo.CourseCode);
                        $('#CourseName').val(courseInfo.CourseName);
                        //$('#TeachingClass').val(courseInfo.TeachingClass);
                        $('#AuditOfficeCode').val(courseInfo.AuditOfficeCode);
                        $('#CourseTypeCode').val(courseInfo.CourseTypeCode);
                        $('#ModifyBy').val(courseInfo.CourseTypeCode);
                        $('#SchoolSmester').val(courseInfo.SchoolSmester);
                        $('#Remark').val(courseInfo.Remark);

                    } else {
                        alert(data.Msg);
                    }
                }
            });
            //打开模态框
            $('#myModal').modal('show')


        }

        //删除菜单
        function deleteMenu(rowId, courseId) {

            console.log(rowId, courseId);

            if (confirm('确认删除吗') == true) {
                $.ajax({
                    type: "POST",
                    url: "/Course/Delete",
                    dataType: "json",
                    data: { "CourseId": courseId },
                    success: function (data) {
                        if (data.IsSuccess) {
                            alert("操作成功！");
                            //隐藏模态框
                            $('#myModal').modal('hide');
                            //重新加载表格
                            loadSiteDataList();
                        } else {
                            alert(data.Msg);
                        }

                    }
                });
            } else {
                return;
            }
        }

        function saveMenu() {
            //debugger

            //获取提交数据
            var CourseId = $('#CourseId').val();
            var CourseCode = $('#CourseCode').val();
            var CourseName = $('#CourseName').val();
            var CourseTypeCode = $('#CourseTypeCode').val();
            var SchoolSmester = $('#SchoolSmester').val();
            var Remark = $('#Remark').val();
            var AuditOfficeCode = $('#AuditOfficeCode').val();
            var ModifyBy = $('#ModifyBy').val();



            $.ajax({
                type: "POST",
                url: "/Course/Submit",
                dataType: "json",
                data: { "CourseId": CourseId, "CourseCode": CourseCode, "CourseName": CourseName, "AuditOfficeCode": AuditOfficeCode, "CourseTypeCode": CourseTypeCode, "SchoolSmester": SchoolSmester, "Remark": Remark, "ModifyBy": ModifyBy, "editFlag": editFlag },
                success: function (data) {
                    if (data.IsSuccess) {
                        alert("操作成功！");
                        //隐藏模态框
                        $('#myModal').modal('hide');
                        //重新加载表格
                        loadSiteDataList();
                    } else {
                        alert(data.Msg);
                    }

                }
            });
        }
        //导入课程
        function AddCourse() {

            //$.ajaxFileUpload({
            //    url: '/Course/ExcelToUpload',
            //    secureuri: false,
            //    fileElementId: 'file',
            //    dataType: 'json',
            //    success: function (data) {
            //        if (data.IsSuccess) {
            //            alert(data.ResultValue);
            //        } else {
            //            alert(data.Msg);
            //        }
            //    },
            //    error: function (data, status, e) {
            //        alert(e);
            //    }
            //});
            //return false;

            var formData = new FormData();
            var filename = $("#file")[0].files[0];
            if (filename == null || filename == "") {
                swal("请选择要导入的文件");
                return;
            }
            if (filename != null) {
                formData.append('file', $("#file")[0].files[0]); //将文件转成二进制形式
                $.ajax({
                    type: "post",
                    url: "/Course/ExcelToUpload",//"/文件名/控制器名/控制器内方法"
                    async: false,
                    contentType: false, //这个一定要写
                    processData: false, //这个也一定要写，不然会报错
                    data: formData,
                    dataType: 'json', //返回类型，有json，text，HTML。这里并没有jsonp格式，所以别妄想能用jsonp做跨域了。
                    success: function (data) {
                        //swal(data);
                        if (data.IsSuccess) {
                            alert(data.ResultValue);
                            //重新刷新当前页面
                            window.location.reload();
                        } else {
                            alert(data.Msg);
                            //重新刷新当前页面
                            window.location.reload();
                        }
                    },
                        error: function (data, status, e) {
                                alert(e);
                            }
                    //error: function (XMLHttpRequest, textStatus, errorThrown, data) {

                    //    swal(errorThrown);

                    //}
                });
            } else {

            }
        }
        //下载课程信息模板
        function DownloadExcel() {

            window.open("/Course/ExportTemplate");
        }
        //跳转页面
        function redirectUrl(url) {
            window.open(url, "_self");
        }
    </script>
}
